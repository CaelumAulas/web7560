{
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "http-server": "^0.11.1"
  },
  "scripts": {
    "start": "babel src/ --out-dir dist/ --copy-files --presets env --watch & npm run server",
    "server": "http-server -c-1 dist"
  }
}
